services:
  fileshare:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./files:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: unless-stopped
